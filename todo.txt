In Order:
    Get create/edit/view to work completely.
    Create default tmpl so each page has a default look instead of just the text and body.
        -(ex: home page link)
    Login screen
        (hashing, encryption/decryption, mysql)


-Prevent overwriting of existing page when creating.
-Prevent creating of new page when editing.
-Create file listing when viewing.
-Login screen w/ HTML <input> tag --> save info into mysql DB.
-Make nicer looking with CSS.
